# 旅行計畫網頁（Claude Code 專案指令）

@AGENTS.md
@HANDOFF.md

## Claude Code 專屬補充
- 本機：唯一真相來源是同層的 `AGENTS.md`（已 gitignore，只在使用者電腦上）。更新專案狀態、踩坑、資料模型時**只改 `AGENTS.md`**。
- 雲端／手機（claude.ai/code）：repo 裡沒有 `AGENTS.md`，請依公開版 `HANDOFF.md` 工作。
- `HANDOFF.md` 與本檔會公開在 GitHub，**不要寫入任何私人內容**。
- `AGENTS.md`、`設定教學_Firebase與上線.md`、`private-backups/` 在 `.gitignore` 內，只留本機。
